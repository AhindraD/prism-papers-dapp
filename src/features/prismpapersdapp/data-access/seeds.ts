
export const USER_SEED = new TextEncoder().encode("user");
export const PAPER_SEED = new TextEncoder().encode("paper");
export const REVIEW_SEED = new TextEncoder().encode("review");
export const RECEIPT_SEED = new TextEncoder().encode("receipt");
export const VAULT_USER_SEED = new TextEncoder().encode("vault_user");
export const VAULT_ADMIN_SEED = new TextEncoder().encode("vault_admin");
